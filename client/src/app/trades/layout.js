import AppLayout from '../../components/AppLayout';

export const metadata = {
  title: 'Trades - TradeSync',
};

export default function Layout({ children }) {
  return <AppLayout>{children}</AppLayout>;
}
