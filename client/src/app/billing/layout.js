import AppLayout from '../../components/AppLayout';

export const metadata = {
  title: 'Billing - TradeSync',
};

export default function Layout({ children }) {
  return <AppLayout>{children}</AppLayout>;
}
